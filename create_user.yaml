- name: users
  hosts: all
  become: yes
  tasks:
  - name: Create users
    user:
        name: "{{ item }}"
        state: present
    with_items:
      - arya
      - sansa
      - tirion
    tags:
      - create_users    